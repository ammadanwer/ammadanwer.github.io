{
  "name": "ammadanwer-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node --test && python3 -B -m unittest discover -s services/voice/tests"
  }
}
