window.AMMAD_AI_CONFIG = Object.freeze({
  enabled: true,
  avatarEnabled: true,
  voiceEnabled: true,
  mode: "remote",
  endpoint: "https://ammad-portfolio-ai.ammad-anwer.workers.dev/api/chat",
  maxQuestionLength: 500,
  maxQuestionsPerSession: 5,
  mockDelayMs: 550,
  remoteTimeoutMs: 15000,
  remoteVoiceTimeoutMs: 150000
});
